# Parsing Go code with `ast`

This folder contains explorations around parsing and reading Go code with the
[`ast`](https://pkg.go.dev/go/ast) and related packages.

* [Read package level comments](package_comment.go#L11)